.container
  .row
    .span6.offset3
      - flash.each do |type, message|
        %div{:class => "alert alert-#{type} fade in"}
          %button.close{"data-dismiss" => "alert"} ×
          = message
