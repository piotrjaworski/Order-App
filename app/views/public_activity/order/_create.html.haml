%span.timeprint
  = timeago_tag activity.created_at, :nojs => false, :limit => 3000.days.ago
%br
= fa_icon "plus"
= activity.owner.name
%strong ordered
- if activity.trackable
  = link_to "#{show_products(activity.trackable)}" , activity.trackable
- else
  which has been destroyed.
